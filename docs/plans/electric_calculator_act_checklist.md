

[ ] **배포된 Azure ML 모델이 current_usage(=에어컨 습관이 흘러드는 유일한 피처)를 무시**-> 백엔드에서 사용자에게 프론트엔드에서 에어컨 사용량 값을 백엔드에 전달 받으면 Virtual_Prev = 136 + (사용시간 × 0.65kW × 30일 × 0.60)  [`136 + (usage_hours * 0.65 * 30 * 0.60):.2f`] 계산식을 통해 ML의 prev_year_usage 컬럼의 null 값을 계산식으로 도출된 값을 채운다.